{
  "name": "sales-contract-audit",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && source venv/bin/activate && uvicorn app.main:app --reload",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "echo '请分别在不同终端运行: npm run dev:backend 和 npm run dev:frontend'"
  }
}
